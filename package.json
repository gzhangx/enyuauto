{
  "name": "enyuauto",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/gzhangx/enyuauto#readme",
  "bugs": {
    "url": "https://github.com/gzhangx/enyuauto/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gzhangx/enyuauto.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/test.js",
    "test": "npm run build && node dist/test.js",
    "package": "npm run build && node scripts/create-zip.js",
    "deploy": "npm run package && aws lambda update-function-code --function-name enyu_auto --zip-file fileb://temp/lambda-deployment.zip --region us-east-1 --no-cli-pager",
    "deploy:guided": "sam build && sam deploy --guided",
    "deploy:quick": "npm run build && sam build && sam deploy"
  },
  "dependencies": {
    "@gzhangx/googleapi": "^0.0.80",
    "axios": "^1.13.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
